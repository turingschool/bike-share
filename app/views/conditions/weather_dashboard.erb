<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
  </head>
  <body>
    <header><h1>Conditions By Day:</h1></header>
    <h4>
      <button href="/conditions/new">NEW</button>
    </h4>
    <% @conditions.each do |weather| %>
    <section>
      <a href="/conditions/<%= weather.id %>"><%= weather.date %></a>
      <a class="temps">max temperature: <%= weather.max_temperature %></a>
      <a class="temps">min temperature: <%= weather.min_temperature %></a>
      <a class="temps">average temperature: <%= weather.mean_temperature %></a>
      <a>humidity: <%= weather.mean_humidity %>&#37;</a>
      <a>visibility: <%= weather.mean_visibility %> miles</a>
      <a>wind speed: <%= weather.mean_wind_speed %>mph</a>
      <a>precipitation: <%= weather.precipitation %> inches</a>
      <button href="/conditions/<%= weather.id %>/edit" value="EDIT"/>
      <form href="/conditions/<%= weather.id %>" method="post">
        <input type="hidden" name="_method" value="DELETE"/>
        <input type="submit" value="DELETE"/>
      </form>
    </section>
  </body>
</html>
