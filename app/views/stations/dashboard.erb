<h1>Station Dashboard</h1>

<h4>Total count of stations: <%= @stations.all.count %></h4>

<h4>Average bikes available per station (based on docks)</h4>


<h4>Most bikes available at a station (based on docks).</h4>
<h4>Fewest bikes available at a station (based on docks).</h4>



<h4>Station(s) where the most bikes are available (based on docks).</h4>
<h4>Station(s) where the fewest bikes are available (based on docks).</h4>

<h4>Most recently installed station: <%=@stations.minimum("installation_date") %></h4>
<h4>Oldest station: <%=@stations.maximum("installation_date") %></h4>
