<h1>Station Dashboard</h1>

<h4>Total count of stations: <%=@all_stations%></h4>

<h4>Average bikes available per station (based on docks): <%=@average%></h4>

<h4>Most bikes available at a station (based on docks): <%=@max_dock_count%></h4>
<h4>Fewest bikes available at a station (based on docks): <%=@min_dock_count%></h4>

<h4>Station(s) where the most bikes are available (based on docks): </h4>

<% @max_dock_name.each do |station|%>
<p> <%= station.name %></p>
<%end%>
<h4>Station(s) where the fewest bikes are available (based on docks): </h4>

<% @min_dock_name.each do |station|%>
<p> <%= station.name %></p>
<%end%>

<h4>Most recently installed station: <%=@recent%></h4>
<h4>Oldest station: <%=@oldest%></h4>
