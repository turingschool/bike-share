<h1><%= @station.name %></h1>
<div class="station info">
    <p>Dock Count: <%= @station.dock_count %></p>
    <p>City: <%= @station.city %></p>
    <p>Installation Date: <%= @station.installation_date %></p>

</div>

<div class="station analytics">
  <h3><%= @station.name %> Analytics</h3>
    <p>Number of rides started at this station: <%= Trip.number_of_starting_rides_at_station(@station.id) %></p>
    <p>Number of rides ending at this station: <%= Trip.number_of_ending_rides_at_station(@station.id) %></p>
    <p>Most frequent destination station from here: <%= Trip.most_frequent_destination(@station.id) %></p>
    <p>Most frequent origination station to here: <%= Trip.most_frequent_origination(@station.id) %></p>
    <p>Date with highest number of origination trip from here: <%= Trip.date_with_highest_trip_from_this_station(@station.id) %></p>
    <p>Most frequent zipcode for users starting trips at this station: <%= Trip.most_frequent_zip_code_users_starting(@station.id) %></p>
    <p>Most frequently use bike starting at this station: <%= Trip.most_frequent_bike_starting(@station.id) %></p>
</div>

<p><a class=class="btn btn-link" href="/stations/<%= @station.id %>/edit">Edit Station</a></p>
<form action="/stations/<%= @station.id %>" method="post">
  <input type="hidden" name="_method" value="delete">
  <input class="btn btn-danger" type="submit" value="Delete Station" />
</form>
