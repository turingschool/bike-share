<!DOCTYPE html>
<html lang="en">

<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <!-- Bootstrap CSS -->
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/overwrite.css" rel="stylesheet">
</head>

<body>
    <nav class="navbar navbar-inverse navbar-sticky-top" role="navigation">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" id="text" href="/">BikeShare</a>
            </div>
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li>
                        <a id="text" href="/stations"><button class="btn btn-primary navbar-btn">Station Index</button></a>
                    </li>
                    <li>
                        <a id="text" href="/cities"><button class="btn btn-primary navbar-btn">City Index</button></a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <table class="table">
      <thead>
        <tr>
          <th>Trip ID</th>
          <th>Duration</th>
          <th>Start Date</th>
          <th>Start Station ID</th>
          <th>End Date</th>
          <th>End Station Id</th>
          <th>Bike ID</th>
          <th>Subscription Type</th>
          <th>Zip Code</th>
          <th>Delete</th>
          <th>Update</th>
        </tr>
      </thead>

      <tbody>
        <!-- <% count = 0 %> -->
        <th scope="row" id="total entries"></th>
        <% @trips.each do |trip| %>
        <tr>
          <td><a href="trips/<%= trip.id %>"><%= trip.id %></a></td>
          <td><%= trip.duration%></td>
          <td><%= trip.start_date%></td>
          <td><%= trip.start_station_id%></td>
          <td><%= trip.end_date%></td>
          <td><%= trip.end_station_id%></td>
          <td><%= trip.bike_id%></td>
          <td><%= trip.subscription_type%></td>
          <td><%= trip.zip_code%></td>
          <td><form class="btn-form" id="trip_<%= trip.id %>" action="/trips/<%=trip.id%>" method="POST">
            <input class="button btn btn-delete" type="hidden" name="_method" value="DELETE">
            <input id="delete_trip" type="submit" value="Delete"/>
          </form></td>
          <td><a id="Update_<%= trip.id %>" class="btn btn-update button" href="/trips/<%=trip.id%>/edit">Update Trip<a/></td>
        </tr>
        <% end %>
      </tbody>

    </table>

    <div class="container">
              <%= will_paginate @trips %>
    </div>
    <footer class="footer">
        <div class="container">
            <p class="text-muted">BikeShare August 17</p>
            <p class="text-muted" id="right">Built with ActiveRecord and Sinatra</p>
        </div>

    </footer>
    <!-- Optional JavaScript -->
    <!-- jQuery first, then Popper.js, then Bootstrap JS -->
    <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js" integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.11.0/umd/popper.min.js" integrity="sha384-b/U6ypiBEHpOf/4+1nzFpr53nxSS+GLCkfwBdFNTxtclqqenISfwAzpKaMNFNmj4" crossorigin="anonymous"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta/js/bootstrap.min.js" integrity="sha384-h0AbiXch4ZDo7tp9hKZ4TsHbi047NrKGLO3SEJAg45jXxnGIfYzk4Si90RDIqNm1" crossorigin="anonymous"></script>
</body>

</html>
