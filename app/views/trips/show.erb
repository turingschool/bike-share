<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Trip Info</title>
    <link rel="stylesheet" type="text/css" href="/reset.css">
    <link rel="stylesheet" type="text/css" href="/main.css">
  </head>
  <body>
    <nav>
      <img class="logo" src="https://bikeshare.metro.net/wp-content/themes/lametro/library/images/logo.png">
      <h1>Trip Info</h1>
    </nav>
    <div class="body-container">
      <div class="individual-container">
        <p>Duration: <%= @trip.duration %></p><br>
        <p>Start Date: <%= @trip.start_date %></p><br>
        <p>Start Station: <%= @trip.start_station.name %></p><br>
        <p>End Date: <%= @trip.end_date %></p><br>
        <p>End Station:<%= @trip.end_station.name %></p><br>
        <p>Bike ID: <%= @trip.bike_id %></p><br>
        <p>Subscription Type: <%= @trip.subscription_type %></p><br>
        <p>Zip Code: <%= @trip.zip_code %></p><br>
        <div class="edit-delete">
          <a class="edit" href="/trips/<%= @trip.id%>/edit">Edit</a>
          <form class="delete" action="/trips/<%= @trip.id %>" method="post">
            <input type="hidden" name="_method" value="DELETE">
            <input class="delete" type="submit" value="Delete">
          </form>
        <div class="edit-delete">
      </div>
    </div>
    <div class="go-back-link">
      <a href="/trips">Go Back to Trips Index</a>
    </div>
  </body>
</html>
