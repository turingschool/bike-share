# specify which database and adapter to use for which environment

development:
  adapter: postgresql
  database: bike-share-development

test:
  adapter: postgresql
  database: bike-share-test

production:
  adapter: postgresql
  database: da4430k5ug62pn
